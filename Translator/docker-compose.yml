version: "3"

services:
  backend:
    container_name: backend
    build: ./backend
    volumes:
      - ./backend:/src
    ports:
      - "5000:5000"
    links:
      - mongo
#  frontend:
#    container_name: frontend
#    build: ./frontend
#    ports:
#      - 8080:8080
#    volumes:
#      - ./frontend:/project
#      - /project/node_modules
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - ./data:/data/db
    restart: always
